<%= form_with(model: pc_i, local: true) do |form| %>
  <% if pc_i.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pc_i.errors.count, "error") %> prohibited this pc_i from being saved:</h2>

      <ul>
      <% pc_i.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :presentacion_congreso_id %>
    <%= form.number_field :presentacion_congreso_id %>
  </div>

  <div class="field">
    <%= form.label :investigador_id %>
    <%= form.number_field :investigador_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
