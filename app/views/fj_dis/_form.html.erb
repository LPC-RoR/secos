<%= form_with(model: fj_di, local: true) do |form| %>
  <% if fj_di.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fj_di.errors.count, "error") %> prohibited this fj_di from being saved:</h2>

      <ul>
      <% fj_di.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :formacion_joven_id %>
    <%= form.number_field :formacion_joven_id %>
  </div>

  <div class="field">
    <%= form.label :disciplina_id %>
    <%= form.number_field :disciplina_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
