<%= form_with(model: rfc_fjo, local: true) do |form| %>
  <% if rfc_fjo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rfc_fjo.errors.count, "error") %> prohibited this rfc_fjo from being saved:</h2>

      <ul>
      <% rfc_fjo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rf_colaboracion_id %>
    <%= form.number_field :rf_colaboracion_id %>
  </div>

  <div class="field">
    <%= form.label :formacion_joven_id %>
    <%= form.number_field :formacion_joven_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
