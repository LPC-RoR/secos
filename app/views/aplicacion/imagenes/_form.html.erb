<%= form_with(model: imagen, local: true) do |form| %>
  <% if imagen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(imagen.errors.count, "error") %> prohibited this imagen from being saved:</h2>

      <ul>
      <% imagen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orden %>
    <%= form.number_field :orden %>
  </div>

  <div class="field">
    <%= form.label :imagen %>
    <%= form.text_field :imagen %>
  </div>

  <div class="field">
    <%= form.label :nota %>
    <%= form.text_field :nota %>
  </div>

  <div class="field">
    <%= form.label :linea_id %>
    <%= form.number_field :linea_id %>
  </div>

  <div class="field">
    <%= form.label :directorio_id %>
    <%= form.number_field :directorio_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
