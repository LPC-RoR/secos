
<%= form_tag(path, :method => "get", id: "search-form") do %>
	<div class="input-group mb-3">
		<%= text_field_tag :search, params[:search], placeholder: "palabras clave", class: 'form-control' %>
		<span class="input-group-btn">
		<%= submit_tag "Buscar", class: 'btn btn-outline-secondary' %>
		</span>
	</div>
<% end %>