<small><%= form.label 'Orden | Paso' %></small></br>
<div class='form-inline'>
	<% if filtro_conditional_field?(@objeto, 'orden') %>
		<%= form.text_field(:orden, size: 5, class: 'form-control' ) %>
	<% end %>
	<% if filtro_conditional_field?(@objeto, 'paso') %>
		<%= form.text_field(:paso, size: 57, class: 'form-control' ) %>
	<% end %>
</div>

<small><%= form.label 'Detalle' %></small></br>
<% if filtro_conditional_field?(@objeto, 'detalle') %>
  	<%= form.text_area :detalle, size: '68x3' %><!--/br-->
<% end %>

<%= form.hidden_field :tutorial_id %>
