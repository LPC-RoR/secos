<%= form_with(model: ad_i, local: true) do |form| %>
  <% if ad_i.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ad_i.errors.count, "error") %> prohibited this ad_i from being saved:</h2>

      <ul>
      <% ad_i.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :actividad_difusion_id %>
    <%= form.number_field :actividad_difusion_id %>
  </div>

  <div class="field">
    <%= form.label :investigador_id %>
    <%= form.number_field :investigador_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
