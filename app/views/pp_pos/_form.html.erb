<%= form_with(model: pp_po, local: true) do |form| %>
  <% if pp_po.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pp_po.errors.count, "error") %> prohibited this pp_po from being saved:</h2>

      <ul>
      <% pp_po.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :producto_pme_id %>
    <%= form.number_field :producto_pme_id %>
  </div>

  <div class="field">
    <%= form.label :publico_objetivo_id %>
    <%= form.number_field :publico_objetivo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
