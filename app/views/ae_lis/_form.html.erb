<%= form_with(model: ae_li, local: true) do |form| %>
  <% if ae_li.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ae_li.errors.count, "error") %> prohibited this ae_li from being saved:</h2>

      <ul>
      <% ae_li.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :articulo_entrevista_id %>
    <%= form.number_field :articulo_entrevista_id %>
  </div>

  <div class="field">
    <%= form.label :linea_investigacion_id %>
    <%= form.number_field :linea_investigacion_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
